workflows:
  my-workflow:
    triggering:
      events:
        - push
        - tag
      branch_patterns: 
        - pattern: '*'
          include: true
      tag_patterns: 
        - pattern: '*'
          include: true
      cancel_previous_builds: true
    scripts:
      - name: say hello
        script: echo "Hello Codemagic!"
